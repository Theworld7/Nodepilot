## v0.2.6

### 变更

- **日志窗口链接可点击**：运行日志中的 http/https 链接显示为蓝色下划线，单击在系统默认浏览器中打开；日志窗口本身不会被导航走，ANSI 颜色保留
- **运行状态同步修复**：修复服务已在运行但项目行仍显示「待运行」的问题（窗口重开、组件重挂载或热更新后前端状态丢失，导致重复启动被拒绝）；现在启动前会向后端同步运行中的服务，重复启动会提示「服务已在运行」并自动纠正界面状态
- **服务状态实时同步**：新增后端运行状态查询，启动/停止/进程退出事件均与界面保持同步

### 安装

Windows 用户下载安装包（二选一）：

- `nodepilot_0.2.6_x64-setup.exe`（NSIS 安装程序）
- `nodepilot_0.2.6_x64_zh-CN.msi`（MSI 安装程序）

双击运行，按提示完成安装即可。安装后从系统托盘启动 nodepilot。
